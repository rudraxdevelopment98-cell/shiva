fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
# real audio analysis (needs ffmpeg on the system: brew install ffmpeg)
librosa>=0.10
soundfile>=0.12
numpy>=1.26
# next: onnxruntime (trained emotion model), pgvector (similarity)
