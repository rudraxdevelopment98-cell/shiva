/* ============================================================
   Shiva Portal — configuration
   Leave these blank to run the LOCAL prototype (data in browser).
   Fill them in (after creating a Supabase project + running the
   SQL in /supabase/schema.sql) to run the REAL multi-user portal.
   See docs/supabase-setup.md for the 10-minute setup.
   ============================================================ */
window.SHIVA_CONFIG = {
  SUPABASE_URL: "",        // e.g. "https://abcd1234.supabase.co"
  SUPABASE_ANON_KEY: "",   // the project's public anon key
};
