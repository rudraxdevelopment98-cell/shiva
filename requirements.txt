# System dependency (install separately): ffmpeg  —  brew install ffmpeg

# Desktop app (native window) + local UI server
flask>=3.0
pywebview>=5.0

# Phase 1 — master chain (EQ / compression / limiting). Optional but recommended.
pedalboard>=0.9

# Phase 2 — reference matching ("make mine sound like this")
matchering>=2.0
